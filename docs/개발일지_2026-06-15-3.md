# 개발일지 — 2026-06-15 (3차)

## 진행 내용

### 보안 강화 — RLS 강화 + UI 권한 제한

---

### 1. 문제 파악

기존 항차 관련 테이블 RLS 정책:

```sql
-- 기존 (취약): 모든 로그인 사용자가 INSERT/UPDATE/DELETE 가능
create policy "staff voyages" on voyages
  for all using (auth.role()='authenticated')
  with check (auth.role()='authenticated');
```

- `auth.role()='authenticated'` 조건은 구글 로그인만 된 누구에게나 쓰기 권한을 부여
- escort(인솔자) 계정도 데이터를 수정·삭제할 수 있는 상태
- `eli_trips` / `eli_passengers` 테이블은 이미 `eli_get_my_role()` 패턴으로 올바르게 보호되어 있었으나, 나중에 생성된 항차 테이블에 동일 패턴이 적용되지 않음

---

### 2. DB 레벨 수정 — RLS 마이그레이션

**파일**: `supabase/migrations/20260615000000_rls_hardening.sql`

대상 테이블 7개에 eli_trips 패턴 적용:

| 테이블 | SELECT | INSERT | UPDATE | DELETE |
|--------|--------|--------|--------|--------|
| voyages | 전체 인증 | admin/staff | admin/staff | admin만 |
| flights | 전체 인증 | admin/staff | admin/staff | admin만 |
| itinerary_days | 전체 인증 | admin/staff | admin/staff | admin만 |
| cancellation_policies | 전체 인증 | admin/staff | admin/staff | admin만 |
| history_logs | 전체 인증 | admin/staff | **없음(append-only)** | admin만 |
| hotels | 전체 인증 | admin/staff | admin/staff | admin만 |
| voyage_flights | 전체 인증 | admin/staff | admin/staff | admin만 |

```sql
-- 예시: voyages 테이블
DROP POLICY IF EXISTS "staff voyages" ON voyages;
CREATE POLICY "read voyages"   ON voyages FOR SELECT USING (auth.role() = 'authenticated');
CREATE POLICY "write voyages"  ON voyages FOR INSERT WITH CHECK (eli_get_my_role() IN ('admin','staff'));
CREATE POLICY "update voyages" ON voyages FOR UPDATE
  USING     (eli_get_my_role() IN ('admin','staff'))
  WITH CHECK(eli_get_my_role() IN ('admin','staff'));
CREATE POLICY "delete voyages" ON voyages FOR DELETE USING (eli_get_my_role() = 'admin');
```

`history_logs`는 기록 무결성을 위해 UPDATE 정책 없음 (append-only).

**적용 방법**: Supabase 대시보드 > SQL Editor에서 마이그레이션 파일 실행

---

### 3. UI 레벨 수정 — escort 편집 버튼 숨김

DB 레벨 보호와 함께, UI에서도 escort 계정에 편집 버튼을 보이지 않게 처리.

`useAuth()` → `canWrite` (`!isEscort`)를 각 컴포넌트에서 확인.

**수정 파일 목록**:

| 파일 | 변경 내용 |
|------|-----------|
| `src/pages/VoyageMaster.tsx` | "새 행사" 버튼 → canWrite일 때만 표시 |
| `src/pages/VoyageSearch.tsx` | canWrite 추출 → 카드 컴포넌트에 prop 전달 |
| `src/components/voyages/tabs/ProductTab.tsx` | "새 행사 등록" + ✏️ + 복제 버튼 → canWrite 조건 |
| `src/components/voyages/tabs/CruiseTab.tsx` | ✏️ 버튼 → canWrite 조건 |
| `src/components/voyages/tabs/FlightsTab.tsx` | ✏️/🗑️/추가 버튼 → canWrite 조건 |
| `src/components/voyages/tabs/HotelTab.tsx` | ✏️/🗑️/추가 버튼 → canWrite 조건 |
| `src/components/voyages/OverviewCard.tsx` | canWrite prop 추가, ✏️ 버튼 조건 |
| `src/components/voyages/FlightsCard.tsx` | canWrite prop 추가, ✏️ 버튼 조건 |
| `src/components/voyages/ItineraryCard.tsx` | canWrite prop 추가, ✏️ 버튼 조건 |
| `src/components/voyages/CancellationCard.tsx` | canWrite prop 추가, ✏️ 버튼 조건 |
| `src/components/voyages/HistoryCard.tsx` | canWrite prop 추가, 메모 입력 영역 조건 |

---

### 4. 역할별 권한 정리

| 기능 | admin | staff | escort |
|------|-------|-------|--------|
| 데이터 조회 | ✅ | ✅ | ✅ |
| 항차/항공/호텔 등록·수정 | ✅ | ✅ | ❌ |
| 히스토리 메모 추가 | ✅ | ✅ | ❌ |
| 항차/호텔 삭제 | ✅ | ❌ | ❌ |
| 사용자 관리 | ✅ | ❌ | ❌ |

---

### 적용 필요 사항

**Supabase SQL Editor에서 아래 마이그레이션 실행 필요**:

```
supabase/migrations/20260615000000_rls_hardening.sql
```

SQL Editor URL: https://supabase.com/dashboard/project/fnrghzzqwergwcssybro/sql/new

---

## 커밋

```
security: RLS 강화(항차 테이블 7개) + UI escort 편집 버튼 숨김
```
