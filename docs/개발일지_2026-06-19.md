# 개발일지 — 2026-06-19

## 진행 내용

1. 대시보드에 항차 운영 데이터 통합
2. 대시보드 전면 개편 — 여행(eli_trips) 중심 → 항차 운영 현황 중심
3. 하단 3개 섹션을 탭 패널로 레이아웃 개선 (후에 원페이지로 재변경)
4. 원페이지 2컬럼 레이아웃으로 재구성 + 판매중 항차 아코디언 적용

---

## 1. 대시보드 항차 데이터 통합

### 배경

기존 대시보드는 `eli_trips` 기반의 여행 일정 통계만 표시.  
항차 관리(VoyageMaster)가 TS 기반으로 별도 구현되어 있었으나 대시보드(JS)에서 항차 데이터를 활용하지 못하고 있었음.  
Vite 환경에서 JS → TS import가 가능하므로 기술적 장벽 없이 통합 가능.

### 추가된 쿼리 함수

`src/lib/queries/voyages.ts`에 `fetchVoyageDashboardData()` 신규 추가.

```typescript
export interface VoyageDashboardData {
  onSaleVoyages: OnSaleVoyage[]          // 판매중 항차 목록
  thisMonthDepartures: DepartureVoyage[] // 이번달 출발 항차
  thisMonthPayments: PaymentRow[]        // 이번달 미완료 결제 마감
}
```

- **판매중 항차**: `status = '판매중'`인 항차 전체, 출발일 오름차순
- **이번달 출발**: 당월 1일~말일 출발, 취소 제외
- **이번달 결제 마감**: `payment_schedules` 당월 `due_date` + `is_completed = false`, `voyages(region, departure_date)` 조인

---

## 2. 대시보드 전면 개편

### 변경 전 → 후

| 항목 | 변경 전 | 변경 후 |
|------|---------|---------|
| 통계 카드 | 예정 여행 / 진행중 여행 / 전체 여행 / 이번달 신규 여행자 | 판매중 항차 / 총 예약 인원 / 이번달 출발 / 이번달 결제 마감 |
| 메인 섹션 | 예정 여행 5건 목록 | 판매중 항차 현황 테이블 |
| 하단 섹션 | 없음 | 이번달 결제 마감 + 이번달 출발 항차 |

### 통계 카드 상세

| 카드 | 색상 | 데이터 | 비고 |
|------|------|--------|------|
| 판매중 항차 | 보라 | `onSaleVoyages.length` | |
| 총 예약 인원 | 파랑 | 판매중 항차 `customer_count` 합산 | |
| 이번달 출발 | 청록 | `thisMonthDepartures.length` | |
| 이번달 결제 마감 | 주황 / 빨강 | `thisMonthPayments.length` | 연체 건수 있으면 빨강 + "연체 N건" 서브텍스트 |

### 판매중 항차 현황 테이블

- 컬럼: 지역 / 선박 / 출발일 / 예약인원 / 잔여 캐빈
- 잔여 캐빈 색상 규칙:
  - `>20%` → 초록
  - `1~20%` → 주황
  - `0석` → 빨강 "마감"

### 연결 링크

| 위치 | 링크 | 이동 대상 |
|------|------|----------|
| 이번달 결제 마감 | "결제 탭 →" | `/voyages?tab=결제` |
| 이번달 출발 항차 | "달력 보기 →" | `/voyages?tab=달력` |

VoyageMaster는 `useSearchParams`로 `tab` 파라미터를 읽으므로 URL 직접 연결 가능.

### 데이터 로딩 개선

기존 `Promise.all` → `fetchVoyageDashboardData()` 단일 호출로 변경.  
항차 API 실패 시 `.catch(() => {})` 처리로 빈 상태 유지, 화면 깨짐 없음.

---

## 3. 탭 패널 → 원페이지 2컬럼 레이아웃으로 재구성

### 탭 패널 시도 및 폐기

3개 섹션을 탭으로 묶었으나 "클릭해서 일일이 봐야 한다"는 단점 지적.  
대시보드 본래 목적(한눈에 현황 파악)에 맞지 않아 원페이지 구성으로 재설계.

### 최종 레이아웃

```
[판매중 항차] [총 예약 인원] [이번달 출발] [이번달 결제 마감]  ← 통계 카드 4개

┌──────────────────────────────┬────────────────────┐
│ 판매중 항차 현황 (3/5)        │ 이번달 결제 마감    │
│  (아코디언 리스트)             ├────────────────────┤
│                              │ 이번달 출발 항차    │
└──────────────────────────────┴────────────────────┘
```

- 데스크톱: `lg:grid-cols-5` — 좌 `col-span-3` / 우 `col-span-2` (결제 마감 + 출발 항차 세로 배열)
- 모바일: 단일 컬럼으로 순서대로 스택

---

## 4. 판매중 항차 아코디언

### 배경

항차 목록이 길어질 경우 테이블 행이 화면을 많이 차지함.  
요약 정보(지역·출발일·예약인원·잔여캐빈)는 항상 표시하고, 상세 정보는 클릭 시 펼치는 아코디언 방식 채택.

### 구현

- 각 행이 `<button>` 으로 동작, `expandedId` state로 한 번에 하나만 펼침
- 펼침 시 표시 항목: **귀항일 / 크루즈사 / 항공사 / 인솔자**
- `ChevronDown` 아이콘이 열림/닫힘 상태에 따라 180° 회전

```jsx
// expandedId 토글 로직
function toggle(id) {
  setExpandedId(prev => prev === id ? null : id)
}
```

- `voyages.ts` select에 `return_date, cruise_line, airline, tour_leader` 추가

---

---

## 5. 히스토리 수정·삭제 + 복원 토스트

### 배경

히스토리 로그가 조회·추가만 가능하고 수정/삭제 불가. 실무 오타 수정이 어려움.

### 구현

- `src/lib/queries/voyages.ts`에 `updateHistoryLog(id, content)`, `deleteHistoryLog(id)` 추가
- `HistoryCard.tsx` (항차 상세 뷰): 각 로그 행 hover 시 연필/휴지통 아이콘 표시
  - 수정: 클릭 시 인라인 textarea 전환, Ctrl+Enter 저장, Escape 취소
  - 삭제: 즉시 삭제 + 캐시에서 낙관적 제거 → `bottom-center` 토스트 3.5초, "복원" 버튼으로 재삽입
- `HistoryTab.tsx` (전체 히스토리 마스터): 테이블 마지막 열에 수정/삭제 액션 열 추가
  - 수정: 내용 셀이 textarea로 변환, 같은 행 액션 열에 저장/취소 버튼
  - 삭제: 동일한 토스트 패턴

### 복원 방식

완전 삭제(hard delete) 후 "복원" 클릭 시 `addHistoryLog`로 재삽입 (원본 content/author 유지, `logged_at`은 현재 시각으로 재생성).

---

## 6. 고객 명단 — 행사·연도 드롭다운 추가

### 배경

기존 고객 명단은 이름·연락처·여권번호 텍스트 검색만 가능. 행사별로 탑승객 목록을 바로 조회하기 불편.

### 구현

- `src/lib/passengers.js`에 `getPassengersByTrip(tripId)` 추가 (trips 조인 포함)
- `src/pages/Passengers.jsx` 개편:
  - 상단 필터 행: [연도 드롭다운] [행사 선택 드롭다운] + [텍스트 검색] [검색] 버튼
  - 연도 드롭다운: `eli_trips.depart_date`에서 연도 목록 자동 생성
  - 행사 드롭다운: 연도 필터 적용된 행사 목록, 선택 시 자동 로드
  - 행사 모드 (`mode='trip'`): 테이블에 행사명 열 없이 선택 행사명 헤더 표시
  - 검색 모드 (`mode='search'`): 텍스트 검색, 행사명·출발일 열 + 여행 보기 링크 표시

---

## 7. 대시보드 '이번달 항차 현황' 업데이트

- `전체 항차 현황` → `이번달 항차 현황` 라벨 변경
- 데이터도 `statusCounts`(전체) → `thisMonthStatusCounts`(`thisMonthDepartures` 기준)로 변경
- `fetchVoyageDashboardData`에서 `statusCounts` 필드 제거 불필요 (Dashboard에서 계산)

---

## 8. 항공 탭 날짜·통화 UI 개선

### 배경

`FlightsTab.tsx`의 수정 폼에서 출발일/도착일이 `<Input type="date">` (네이티브 브라우저 캘린더), 통화가 스타일 미적용 select로 표시되어 페이지 디자인과 불일치.

### 변경

- 출발일/도착일: `<Input type="date">` → 커스텀 `DatePicker` 컴포넌트로 교체
- 통화 select: `<Select>` + `appearance-none` + `<ChevronDown>` 아이콘 래퍼로 개선

---

## 변경 파일 목록

| 파일 | 변경 내용 |
|------|-----------|
| `src/lib/queries/voyages.ts` | `fetchVoyageDashboardData()` 신규, `updateHistoryLog`, `deleteHistoryLog` 추가 |
| `src/pages/Dashboard.jsx` | 전면 개편 + '이번달 항차 현황' 데이터 변경, 항차 링크 바로가기 |
| `src/components/voyages/HistoryCard.tsx` | 수정·삭제 + 복원 토스트 추가 |
| `src/components/voyages/tabs/HistoryTab.tsx` | 수정·삭제 + 복원 토스트 추가 |
| `src/lib/passengers.js` | `getPassengersByTrip` 추가 |
| `src/pages/Passengers.jsx` | 연도·행사 드롭다운 추가, 행사 선택 시 자동 로드 |
| `src/components/voyages/tabs/FlightsTab.tsx` | 날짜 DatePicker, 통화 커스텀 select |
| `src/components/Sidebar.jsx` | '여행 일정' 제거, '고객 명단' 이름 변경 |
| `package.json` | `sonner` 패키지 추가 |
